var ready;
ready = function () {

$('#new_recipe_ingredient').remove();
$('#new_link').show();
$('#incomplete_tasks').append('<%= j render(@recipe_ingredient) %>');

};

$(document).ready(ready);
$(document).on('page:load', ready);
$(document).on('turbolinks:render', ready);